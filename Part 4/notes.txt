

- contrainte : pour les IP d'interfaces connectées à un routeur, le masque ne doit pas être trop petit (2 blocs :XXXX:XXXX réservés à l'hôte) (donc /96 max ?)

- contrainte : peering prefix le même pour toutes les liaison d'un couple d'AS => ne supporte pas 2 interfaces vers le même routeur du même AS voisin

- contrainte : peering prefix de l'AS A vers l'AS B le même que de l'AS B vers l'AS A (PAS vérifié automatiquement par le script)
